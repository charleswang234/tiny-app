<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
  <table>
      <tr>
        <td>URL</td>
        <td>shortened</td>
      <tr>
      <% for (shortened in urls) { %>
        <td><%= urls[shortened]%></td>
        <td><%= shortened %> </td>
      </tr>
      <% }; %>
  </table>

  <a>hi </a>
</body>
</html>